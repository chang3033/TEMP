#include <stdio>

int main(argc, argv) {
    for (auto &&i : v)
    {
        
    }
    
    for (size_t i = 0; i < count; i++)
    {
        /* code */
    }
    
    for (int i = length - 1; i >= 0; i--)
    {
        /* code */
    }
    


}
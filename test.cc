#include <stdio>

class C
{
private:
    /* data */
    int y;
public:
    C(int x);
    ~C();
};

C::C(int x)
{
    y = f(x);
}

C::~C()
{
}


int main(argc, argv) {
    for (auto &&i : v)
    {
        
    }
    
    for (size_t i = 0; i < count; i++)
    {
        /* code */
    }
    
    for (int i = length - 1; i >= 0; i--)
    {
        /* code */
    }
    


}
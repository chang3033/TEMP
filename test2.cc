#include <stdio>
#include "function.cc"

class myClass
{
private:
    int y;
public:
    myClass(int x);
    ~myClass();
};

myClass::myClass(int x)
{
    y = f(x);
}

myClass::~myClass()
{
}



int main(argc, argv)
{
    myClass c();

    for (int i = length - 1; i >= 0; i--)
    {
        /* code */
        fopen("hello.dat")
    }

    for (size_t i = 0; i < count; i++)
    {
        int y = f(i);
    }

    int z = f(100);
}